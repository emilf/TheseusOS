[package]
name = "theseus-kernel"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "kernel"
path = "src/main.rs"

[dependencies]
theseus-shared = { path = "../shared" }
log = { workspace = true }
acpi = { workspace = true }
uefi-raw = "0.1.0"


# Release profile defined in workspace root
